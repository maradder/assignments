function increment(initialCount, step) {
  return initialCount + step;
}

function decrement(initialCount, step) {
  return initialCount - step;
}

module.exports = { increment, decrement };
