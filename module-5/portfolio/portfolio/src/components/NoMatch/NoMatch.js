import React, { useState, useEffect } from "react"
import { SectionContainerStyle } from "../StyledComponents"

const NoMatch = () => {
	return (
		<SectionContainerStyle>
			<h1>Sorry, that page doesn't exist</h1>
		</SectionContainerStyle>
	)
}

export default NoMatch
